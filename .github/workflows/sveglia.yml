name: Sveglia Mattutina App
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch: 

jobs:
  sveglia:
    runs-on: ubuntu-latest
    steps:
      - name: Simulazione accesso umano
        run: |
          curl -I -L -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" "https://escursione-sospensioni.streamlit.app/"
